<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.soco.learnandroid">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>5
        <activity android:name=".QuizActivity" />
        <activity android:name=".QuizInitialise" />
        <activity android:name=".ActivitiesLesson" />
        <activity android:name=".Lesson2" />
        <activity android:name=".Lesson3" />
        <activity android:name=".Lesson4" />
        <activity android:name=".Lesson5"></activity>
    </application>

</manifest>